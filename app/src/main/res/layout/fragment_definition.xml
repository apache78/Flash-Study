<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="edu.washington.apache78.FlashStudy.fragments.DefinitionFragment">

    <!-- TODO: Update blank fragment layout -->
    <TextView android:layout_width="match_parent" android:layout_height="464dp"
        android:id="@+id/definitionText"
        android:gravity="center"
        android:text="Definition"
        android:textSize="25dp" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Back to Flash Cards"
        android:id="@+id/btnDone"
        android:layout_gravity="center_horizontal|bottom"
        android:visibility="gone" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Next Term"
        android:id="@+id/btnDefNextTerm"
        android:background="@color/main_app_color"
        android:textColor="#FFFFFF"
        android:layout_gravity="center_horizontal|bottom" />

</FrameLayout>
